{% extends 'WibelluleBlogBundle::layout.html.twig' %}

{% block body %}

    {{ parent() }}

    <div class="section scrollspy" id="detailArticle">
        <div class="container">
            <div class="row">
                <div class="col s12 m12 l12">
                    <div class="card">
                        <div class="card-content">
                            <span class="card-title activator grey-text text-darken-4">{{ article.nom }} <i class="mdi-navigation-more-vert right"></i></span>
                            <p>{{ article.contenu }}</p>
                            <br><br><br>
                            <blockquote>Article écrit par {{ article.auteur }} le {{ article.date|date('d/m/Y') }}</blockquote>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

{% endblock %}